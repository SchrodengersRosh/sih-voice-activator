# Contributing

Use Python 3.11 or 3.12. Create focused changes, preserve the frozen protocol, add/adjust automated tests with every behavior change, run `pytest`, and inspect `git diff` and `git status` before committing. Use conventional scoped messages such as `feat(m1): add websocket receiver`.
